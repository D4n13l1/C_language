#include <stdio.h>
int main()
{
    int trab, hor;
    float val, sal;
    scanf("%d" "%d" "%f", &trab, &hor, &val);
    sal =  val*hor;
    printf("NUMBER = %d\n""SALARY = U$ %.2f\n", trab, sal);
    return 0;
}